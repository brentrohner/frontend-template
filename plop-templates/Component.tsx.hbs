import React from 'react';
import classNames from 'classnames';
import styles from './{{pascalCase name}}.module.scss';

export type {{pascalCase name}}Props = {
  className?: string;
};

export default function {{pascalCase name}}({ className }: {{pascalCase name}}Props): JSX.Element {
  return <div className={classNames(styles.{{~pascalCase name~}}, className)} />;
}
